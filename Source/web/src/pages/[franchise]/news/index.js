import React from 'react';
import { useDispatch } from 'react-redux';

const propTypes = {};

const SiteNews = (props) => {
    //! State
    const dispatch = useDispatch();

    //! Function

    //! Render
    return <div>siteNews</div>;
};

SiteNews.propTypes = propTypes;
export default SiteNews;
